import React from 'react';

import { MenuContainer, MenuWrapper } from './styled';

export const Menu = () => {
  return(
    <MenuContainer>
      <MenuWrapper>menu</MenuWrapper>
    </MenuContainer>
  )
}